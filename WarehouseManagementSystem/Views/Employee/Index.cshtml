@model IEnumerable<EmployeeViewModel>

<h2 class="mt-5 text-center">Employees</h2>

<form method="post" class="mt-5 mr-2">
    <div class="row form-group ">
        <div class="col-md-2">
            <a asp-action="Create" asp-controller="Employee" class="btn btn-primary">Create New</a>
        </div>
    </div>
</form>

<table class="table table-hover table-striped table-borderless mt-5 shadow p-3 mb-5 bg-white rounded table-responsive-lg table-responsive-md table-responsive-sm">
    <thead>
        <tr>
            <th>
                Full Name
            </th>
            <th>
                Card Id
            </th>
            <th>
                Home Warehouse
            </th>
            <th>
                Action
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var employee in Model)
         {
            <tr>
                <td><a asp-controller="Employee" asp-action="Detail" asp-route-id="@employee.Id">@employee.FirstName @employee.LastName</a></td>
                <td class="col-wrap-text">
                    @Html.DisplayFor(modelItem => employee.WarehouseEmployeeCardId)
                </td>
                <td class="col-wrap-text">
                    @Html.DisplayFor(modelItem => employee.HomeWarehouse)
                </td>
                <td class="d-flex justify-content-between" style="max-width:200px;">
                    <a asp-action="Edit" asp-route-id="@employee.Id" class="btn btn-primary btn-sm sameSizeActionsBtn"><i class="fas fa-edit"></i></a>
                    <a asp-action="Detail" asp-route-id="@employee.Id" class="btn btn-secondary btn-sm sameSizeActionsBtn"><i class="fas fa-info"></i></a>
                    <a asp-action="Delete" asp-route-id="@employee.Id" class="btn btn-danger btn-sm sameSizeActionsBtn"><i class="fas fa-trash-alt"></i></a>
                </td>
            </tr>
         }
    </tbody>
</table>